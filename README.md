# PeDitXOS Web Terminal Installer

A minimal OpenWrt-based bootable system for x86-64, built with GitHub Actions.

- Starts TTYD terminal on boot
- No LuCI or SSH exposed
- Web-based install from `setup.sh`
- Built-in drivers for most NICs
- LAN bridge on `10.1.1.1` with DHCP server
